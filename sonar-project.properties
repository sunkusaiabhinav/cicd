# =============================================================
# FILE: sonar-project.properties
# PURPOSE: Configuration for SonarCloud code quality analysis.
#
# WHAT IS SONARCLOUD?
#   SonarCloud is a cloud-based code quality and security analysis tool.
#   It scans your Python code for:
#     - Bugs and code smells
#     - Security vulnerabilities and hotspots
#     - Code duplications
#     - Test coverage gaps
#
# HOW IT WORKS IN CI:
#   The SonarCloud GitHub Action reads this file, runs analysis on your
#   code, and publishes results to sonarcloud.io dashboard.
#
# SETUP REQUIRED:
#   1. Go to https://sonarcloud.io and sign in with GitHub
#   2. Click "+" → "Analyze new project" → select sunkusaiabhinav/cicd
#   3. Get your SONAR_TOKEN from My Account → Security
#   4. Add SONAR_TOKEN as a GitHub Actions secret
# =============================================================

# Unique identifier for this project on SonarCloud
# Format: <github_org>_<repo_name>
sonar.projectKey=sunkusaiabhinav_cicd

# Your SonarCloud organization (same as your GitHub username/org)
sonar.organization=sunkusaiabhinav

# Human-readable project name shown on the SonarCloud dashboard
sonar.projectName=cicd-fastapi-backend

# Project version (optional but useful for tracking quality over time)
sonar.projectVersion=1.0

# -------------------------------------------------------------------
# Source & Test Paths
# -------------------------------------------------------------------
# sonar.sources: directories containing production code to analyze
sonar.sources=app,main.py

# sonar.tests: directories containing test code (analyzed separately)
sonar.tests=tests

# -------------------------------------------------------------------
# Python-Specific Settings
# -------------------------------------------------------------------
# Tell SonarCloud which Python version to use for analysis rules
sonar.python.version=3.11

# Path to the coverage XML report generated by pytest --cov-report=xml
# SonarCloud reads this to display line-by-line coverage on the dashboard
sonar.python.coverage.reportPaths=coverage.xml

# -------------------------------------------------------------------
# Exclusions — files/folders to skip during analysis
# -------------------------------------------------------------------
sonar.exclusions=.venv/**,dist/**,build/**,**/__pycache__/**,*.egg-info/**
